<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mayanktripathi.smartgrid">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/saga_logo_home"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondLogin" />
        <activity android:name=".pie_energy" />
        <activity android:name=".LineGraph" />
        <activity android:name=".Energy" />
        <activity android:name=".appliances" />
        <activity android:name=".Ticker" />
        <activity android:name=".MapsActivity" />
        <activity android:name=".UserCounter" />
        <activity android:name=".suggestions" />
        <activity android:name=".AnimateGraph"/>
    </application>

</manifest>